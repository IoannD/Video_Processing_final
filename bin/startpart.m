function startpart
global start

start.mainwindow = uifigure('Name', 'Обработка капель v3.0',...
    'Position', [500 500 600 400], 'Resize','on','AutoResizeChildren', 'on');

deform_pr_btn = uibutton(start.mainwindow,'Push',...
    'Position', [150 325 300 50],'Text', 'Первичная обработка деформации',...
    'FontSize', 15,'ButtonPushedFcn',@(deform_pr_btn, event) deform_pr_btn_fcn);
deform_res_btn = uibutton(start.mainwindow, 'Push',...
    'Position', [150 225 300 50],'Text', 'Просмотр результатов деформации',...
    'FontSize', 15, 'ButtonPushedFcn',@(deform_res_btn, event) deform_res_btn_fcn);
coal_pr_btn = uibutton(start.mainwindow,'Push',...
    'Position', [150 125 300 50],'Text', 'Первичная обработка коалесценции',...
    'FontSize', 15, 'ButtonPushedFcn',@(coal_pr_btn, event) coal_pr_btn_fcn);
coal_res_btn = uibutton(start.mainwindow, 'Push',...
    'Position', [150 25 300 50],'Text', 'Просмотр результатов коалесценции',...
    'FontSize', 15);
end

function deform_pr_btn_fcn
deformation
end

function deform_res_btn_fcn
deformation_result
end